function make_telethon_figures(varargin)
    % If just one object, will assume comparing shifted to unshifted, if
    % two objects will compare them in the state they are passed (i.e.
    % won't change the 'selected' state)
    
    % for now, just takes one object...
    if nargin == 1;
    elseif nargin == 2;
    end
    
    
    
end